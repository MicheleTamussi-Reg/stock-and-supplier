function app(){
    // richiamo i moduli necessari al funzionamento
    const nomeP = require('./sceltaNome'); 
    const qntP = require('./sceltaQnt');
    const nomeInStock = require('./nomePStock');   
 } 
 app();